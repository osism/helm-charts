{{- if .Values.ingress_banderctl.enabled -}}
{{- $fullName := include "banderctl.fullname" . -}}
{{- $svcPort := .Values.service_banderctl.port -}}
{{- if and .Values.ingress_banderctl.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress_banderctl.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress_banderctl.annotations "kubernetes.io/ingress.class" .Values.ingress_banderctl.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: "{{ include "banderctl.fullname" . }}"
  labels:
    {{- include "bandersnatch.labels" . | nindent 4 }}
  {{- with .Values.ingress_banderctl.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress_banderctl.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress_banderctl.className }}
  {{- end }}
  {{- if .Values.ingress_banderctl.tls }}
  tls:
    {{- range .Values.ingress_banderctl.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }} 
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress_banderctl.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
