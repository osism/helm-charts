apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bandersnatch.fullname" . }}-test-connection"
  labels:
    {{- include "bandersnatch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bandersnatch.fullname" . }}:{{ .Values.service_banderctl.port }}']
  restartPolicy: Never
