apiVersion: v1
kind: Service
metadata:
  name: {{ include "hound-search.fullname" . }}
  labels:
    {{- include "hound-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hound-search.selectorLabels" . | nindent 4 }}
