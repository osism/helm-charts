apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wormhole.fullname" . }}-test-connection"
  labels:
    {{- include "wormhole.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wormhole.fullname" . }}:{{ .Values.service_wormhole.port }}']
  restartPolicy: Never
